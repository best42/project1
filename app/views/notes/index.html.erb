<div class="container">
  <div class="center">
    <div class="row">
      <img class="col-xs-6" src="https://activerain-store.s3.amazonaws.com/image_store/uploads/7/7/0/3/2/ar134783823023077.jpg" class="responsive-img" style="max-height: 300px;"/>
      <h3 class="col-xs-6">List of Notes</h3>
    </div>
  </div>

  <p id="notice"><%= notice %></p>

  <div class="row">
    <% @notes.each do |note| %>
      <div class="col s12 m6 l4">
        <div class="card">
          <div class="card-image">
            <img class="img_fix" src="">
          </div>
          <div class="card-content">
            <p>Title: <%= note.title %></p>
            <p>Subject: <%= note.subject %></p>
            <p>Teacher: <%= note.teacher %></p>
            <p class="text-right">Owner: <%= note.owner %></p>
          </div>
          <div class="card-action">
            <a class ="blue-text text-darken-2" href="/home">Read More</a>
          </div>
        </div>

      </div>
    <% end %>

  <br>
</div>

<%if model_signed_in? %>
  <div class="center">
    <a class="waves-effect waves-light btn-large" <%= link_to 'New Note', new_note_path %></a>
  </div>
<% end %>

<!--
<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Subject</th>
      <th>Teacher</th>
      <th>Rating</th>
      <th>Owner</th>
      <th>Create note</th>
      <th>Image note</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @notes.each do |note| %>
      <tr>
        <td><%= note.title %></td>
        <td><%= note.description %></td>
        <td><%= note.subject %></td>
        <td><%= note.teacher %></td>
        <td><%= note.rating %></td>
        <td><%= note.owner %></td>
        <td><%= note.create_note %></td>
        <td><%= note.image_note %></td>
        <td><%= link_to 'Show', note %></td>
        <td><%= link_to 'Edit', edit_note_path(note) %></td>
        <td><%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table> -->
